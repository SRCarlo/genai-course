# Express Routing

Routing determines how an Express application responds to requests for specific paths and HTTP methods.

Express supports methods such as app.get(), app.post(), app.put(), app.patch(), and app.delete().

Routes can also be organized with express.Router().
