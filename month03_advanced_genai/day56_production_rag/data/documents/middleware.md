# Express Middleware

Express middleware functions execute during the request-response lifecycle.

Middleware receives req, res, and next.

Middleware can inspect requests, modify request or response objects, terminate a request, or call next() to continue.

Common middleware examples include logging, authentication, JSON parsing, validation, and error handling.
