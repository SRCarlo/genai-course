# Asynchronous JavaScript

JavaScript supports asynchronous programming with callbacks, promises, and async/await.

A Promise represents the eventual completion or failure of an asynchronous operation.

The async keyword defines an asynchronous function.

The await keyword pauses execution of an async function until a Promise settles.
